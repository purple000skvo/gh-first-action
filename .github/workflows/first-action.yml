name: First Workflow

on: workflow_dispatch #manually start the worklflow

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: |
          echo "First output"
          echo "Second output"
      - name: print goodbye
        run: echo "Done - bye!"
      
